<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>Hostler</title>
    <link rel="stylesheet" href="index.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">    
  </head>
<body class="container mt-5">
    <div class="head">
        <h1 class="text-danger">Complain List</h1>
        <a href="http://localhost:3300/posts/new" class="btn btn-success btn-lg" style="width: 150px;">
            <span><i class="fas fa-plus"></i></span>  Do Complain!</a>
    </div>
    <% for(post of posts) { %>
        <div class="bg-warning">
            <!-- <h2 class="text-lead bg-info"><%= post.username %></h2>
            <h3 class="bg-danger"><%= post.content %></h3> -->
            <div>
                <h2 class="text-danger">Complain Received from Room No. <%= post.RoomNo %></h3>
            </div>

            <div class="d-flex">
                <a href="http://localhost:3300/posts/<%= post.id %>" class="btn btn-primary btn-lg" style="width: 150px;"><span><i class="fas fa-eye"></i></span>  See Details!</a>
                <a href="http://localhost:3300/posts/<%= post.id %>/edit" class="btn btn-warning btn-lg" style="width: 150px;">
                     <span><i class="fas fa-edit"></i></span>  Edit</a>
 
                <form method="post" action="/posts/<%= post.id%>?_method=DELETE">
                    <button class="btn btn-danger btn-lg" style="width: 150px;"><span><i class="fas fa-trash-alt"></i></span>  Delete</button>
                </form>
            </div>
            <i class="bi bi-trash3"></i>
         </div>
    <%}%>
 </body>
</html>