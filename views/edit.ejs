<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" type="text/css" href="http://localhost:3300/edit.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body class="container">
    <div class="edit">
        <h2 class="text-warning">Edit your Complain</h2>
        <p class="text-muted">post id : <%= post.id %></p>
        <h3>Student Name : <%= post.username %></p>
        <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
            <div class="form-group">
                <label for="Problem">Problem</label>
                <textarea name="content" class="form-control" rows="8" style="font-size: 20px; color: green;"><%= post.content %></textarea>
            </div>
        <button class="btn btn-warning btn-lg">Make Changes!</button>
        </form>
    </div>

</body>
</html>